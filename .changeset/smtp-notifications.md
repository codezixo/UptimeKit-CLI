---
'uptimekit': minor
---

Add SMTP-based email notifications with global SMTP connection settings and per-monitor recipient lists. Configure via `uptimekit notif smtp`, set per-monitor recipients with the new `--smtp-to` flag on `add`/`edit`, and verify with `uptimekit notif smtp test`.